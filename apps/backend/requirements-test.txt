# Minimal requirements for running unit tests in CI
# This excludes heavy ML dependencies (docling, torch, transformers)
# that are only needed for PDF processing and RAG evaluation

# Core framework
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Azure SDK (lightweight - no ML)
azure-search-documents>=11.6.0b8
azure-identity>=1.15.0
azure-storage-blob>=12.27.1
azure-core>=1.30.0

# Azure AD token validation
pyjwt[crypto]>=2.9.0
cachetools>=5.3.0
cryptography>=42.0.0

# OpenAI SDK
openai>=1.55.0

# Retry logic
tenacity>=8.2.0

# HTTP
requests>=2.31.0

# Rate limiting
slowapi>=0.1.9

# Circuit breaker
pybreaker>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.27.0
