# =============================================================================
# Production Runtime Dependencies
# =============================================================================
# These are required to run the FastAPI application in production.
# Evaluation, testing, and ingestion-only deps are in separate files.

# FastAPI application server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Azure AI Foundry SDK (unified AI platform)
azure-ai-projects>=1.0.0b10
azure-ai-agents>=1.1.0
azure-search-documents>=11.6.0b8
azure-identity>=1.15.0
azure-storage-blob>=12.27.1

# Azure Communication Services (for email notifications)
azure-communication-email>=1.0.0

# Azure AD token validation dependencies
pyjwt[crypto]>=2.9.0
cachetools>=5.3.0

# OpenAI SDK (fallback for direct Azure OpenAI calls)
openai>=1.55.0

# Cohere Rerank (cross-encoder reranking for negation-aware search)
cohere>=5.0.0

# PDF processing (Docling is the primary chunker)
docling>=2.15.0
docling-core>=2.21.0
# PyMuPDF used for accurate checkbox/form extraction (fixes Docling table truncation)
pymupdf>=1.24.0

# Retry logic for transient failures
tenacity>=8.2.0

# HTTP
requests>=2.31.0
httpx>=0.27.0
# Use system certificate store (fixes Netskope/corporate proxy SSL issues)
truststore>=0.9.0

# Rate limiting
slowapi>=0.1.9

# Circuit breaker pattern for resilience
pybreaker>=1.0.0

# System resource detection for ingestion tuning
psutil>=6.1.0,<8.0.0

# Observability (Azure AI Foundry tracing + OpenTelemetry)
azure-monitor-opentelemetry>=1.6.0,<2.0.0
azure-ai-inference>=1.0.0b7,<2.0.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-openai>=0.29.0,<1.0.0

# Prometheus metrics
prometheus-fastapi-instrumentator>=7.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
